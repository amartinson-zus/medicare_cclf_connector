vbca: &local_duckdb_profile
  target: ci
  outputs:
    ci:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', '/tmp/medicare_cclf_connector_ci.duckdb') }}"
      schema: "{{ env_var('DBT_DUCKDB_SCHEMA', 'main') }}"
      threads: 4

default: *local_duckdb_profile
