[project]
name = "medicare-cclf-connector"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-core==1.10.1",
    "dbt-duckdb==1.10.1",
    "duckdb==1.4.3",
    "dbt-snowflake>=1.10.1",
]
